@model List<OpcionalViewModel>

<h1 class="title has-text-centered">Opcionais</h1>

<a href="/opcional/cadastrar" class="button is-primary mb-3 is-small is-pulled-right">
    <span class="icon is-small">
        <i class="fas fa-plus"></i>
    </span>
    <span>Cadastrar</span>
</a>

<table class="table is-striped is-bordered is-fullwidth is-hoverable is-primary">
    <thead>
        <tr>
            <th>Código</th>
            <th>Nome</th>
            <th>Ação</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var opcionalViewModel in Model)
        {
            <tr>
                <td>@opcionalViewModel.Id</td>
                <td>@opcionalViewModel.Nome</td>
                <td>
                    <a href="/opcional/editar?id=@opcionalViewModel.Id" class="button is-primary is-small">
                        <span class="icon is-small">
                            <i class="fas fa-pencil"></i>
                        </span>
                        <span>Editar</span>
                    </a>
                    <a href="/opcional/apagar?id=@opcionalViewModel.Id" class="button is-danger is-outlined is-small">
                        <span class="icon is-small">
                            <i class="fas fa-trash"></i>
                        </span>
                        <span>Apagar</span>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>